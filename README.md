Example of testing a web service with QuickCheck
================================================

This project shows how a web service written in Go can be tested using a clone
of Erlang's QuickCheck package called PropEr.

To try it out, run

	make test
